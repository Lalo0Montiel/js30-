// Crear una función que evalúe si un número es par o impar //

const numParImopar = (n) => {
    (n % 2 === 0) ? console.log(`EL número ${n} es par`) : console.log(`EL número ${n} es impar`);
}

let numero = numParImopar(7)